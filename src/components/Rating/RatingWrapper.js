import styled from 'styled-components';

const RatingWrapper = styled.div`
	display: flex;
	align-items: center;

	[role="img"] {
		cursor: pointer;
		font-size: 1.6rem;
	}
`;

export default RatingWrapper;
